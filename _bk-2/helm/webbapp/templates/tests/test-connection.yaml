apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "webbapp.fullname" . }}-test-connection"
  labels:
    {{- include "webbapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "webbapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
